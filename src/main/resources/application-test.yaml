spring:
  application:
    name: AssetFlow
  datasource:
    url: jdbc:h2:mem:assetflow_test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driverClassName: org.h2.Driver
    username: postgres
    password: changemeinprod!
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

application:
  security:
    jwt:
      secret-key: ${JWT_SECRET_KEY:89QbfB+oHv++BngCC4XH9LYrbRxvMTjzZfQoS4MiWt4=}
      expiration: 900000
      refresh-token:
        expiration: 604800000
      cookie:
        path: /
        secure: false
        same-site: Lax

